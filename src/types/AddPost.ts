export type AddPost = {
  body: string;
  image?: File | null; // Optional file object for FormData upload
};

export type AddPostForm = {
  body: string;
  image?: File | null; // Optional file object for FormData upload
};
